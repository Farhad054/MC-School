package com.mcschool.flashcard.study;

public enum SessionType {
    /** Mandatory homework over the cards due today; completing it advances the SM-2 schedule. */
    SCHEDULED,
    /** Voluntary "practice now" run; never changes the schedule (PRD 4.5). */
    PRACTICE
}
